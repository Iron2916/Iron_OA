server:
  port: 8800  # 设置页面访问端口号
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 查看日志
spring:

  datasource: # mybatis 设置
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/iron-oa?serverTimezone=GMT%2B8&useSSL=false&characterEncoding=utf-8&allowPublicKeyRetrieval=true
    username: root
    password: admin123

  jackson: # 设置时间格式
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
